#!/bin/sh

city=$1
url="https://wttr.in/$city?format=3"

curl -Ss $url >$XDG_RUNTIME_DIR/wthr

sig=1
while [ $sig -eq 1 ]
do
    if [ -n $(pgrep i3blocks) ]
    then
        pkill -SIGRTMIN+11 i3blocks
        sig=0
    fi
done

exit 0
